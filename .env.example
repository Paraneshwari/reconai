# Optional. If left blank, ReconAI runs fully offline using deterministic
# fallback explanation templates instead of an LLM. The core reconciliation
# engine and metrics work identically either way.
OPENAI_API_KEY=
